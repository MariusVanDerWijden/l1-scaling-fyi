# Ethereum L1 Gas Limit Scaling Dashboard

A static web dashboard for visualizing Ethereum L1 gas limit scaling metrics and roadblocks.

## Running Locally

```python
python3 -m http.server 8000
```

Then open [http://localhost:8000](http://localhost:8000) in your browser.

